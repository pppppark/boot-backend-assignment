<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.park.mybatis.mybatis.mapper.PostMapper">
    <insert id="createPost" parameterType="dev.park.mybatis.mybatis.dto.PostDto">
        insert into POST(title, content, writer, board)
        values (#{title}, #{content}, #{writer}, ${board})
        <!-- #{title} == title -->
    </insert>
    <insert id="createPostAll"
            parameterType="dev.park.mybatis.mybatis.dto.PostDto">
        insert into POST(title, content, writer, board)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.title}, #{item.content}, #{item.writer}, ${item.board})
        </foreach>
    </insert>
    <select
            id="readPost"
            parameterType="int"
            resultType="dev.park.mybatis.mybatis.dto.PostDto">
        select * from post where id = ${id}
    </select>
    <select id="readPost" resultType="dev.park.mybatis.mybatis.dto.PostDto">
        select * from post
    </select>
    <select
            id="readPostQuery"
            parameterType="dev.park.mybatis.mybatis.dto.PostDto"
            resultType="dev.park.mybatis.mybatis.dto.PostDto">
        select * from post
        where id = ${id}
        <if test="title != null">
            and title = #{title}
        </if>
        <if test="writer != null">
            and writer = #{writer}
        </if>
    </select>
    <select id=""
    <update id="updatePost" parameterType="dev.park.mybatis.mybatis.dto.PostDto">
        update post
        set
            title = #{title},
            content = #{content},
            writer = #{writer},
            board = ${board}
        where id = ${id}
    </update>
    <delete id="deletePost" parameterType="int">
        delete from post where id = ${id}
    </delete>
</mapper>